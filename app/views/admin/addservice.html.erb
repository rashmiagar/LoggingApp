
<div id="bottompane">
	<span class = "help-inline"><%= flash[:notice] %> </span>
	<%= error_messages_for :newservice %>
	<h2> Add Service </h2>
	<%# form_tag( {:controller => 'admin', :action => "add_service" }, {:id => 'add_service_form', :name => 'add_service_form'} ) do %>
	<%#= @newservice.errors.full_messages %>
	
	<% form_for(:types_of_service, :url => '/services', :method => :post) do |f| %>  <!--goes to ServicesController.create()-->
	<p>
		<span style="float:left;margin-right:10px"><label>Service Name:</label></span> <%= f.text_field :service_name %>
	</p>
	<%= submit_tag 'Add Service' %>
	<% end %>
	<%= render :partial => 'viewservice', :locals => {:servicetypes => TypesOfService.all} %>